%h1 Listing attachments

%table
  %thead
    %tr
      %th Attachment
      %th

  %tbody
    - @attachments.each do |attachment|
      %tr
        %td= image_tag attachment.file.to_s
        %td= link_to 'Destroy', attachment_path(attachment), method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Attachment', new_entry_attachment_path(@entry)

