%p
  %strong Title:
  = @blog.title

= link_to 'Edit', edit_blog_path(@blog)
\|
= link_to 'Back', blogs_path

%h1 Listing entries

%table
  %thead
    %tr
      %th Title
      %th Body
      %th{colspan: "3"}

  %tbody
    - @blog.entries.each do |entry|
      %tr
        %td= entry.title
        %td= entry.body
        %td= link_to 'Show', entry_path(entry)
        %td= link_to 'Edit', edit_entry_path(entry)
        %td= link_to 'Destroy', entry_path(entry), method: :delete, data: {confirm: 'Are you sure?' }

%br
  = link_to 'New Entry', new_blog_entry_path(@blog)